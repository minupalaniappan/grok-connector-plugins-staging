# Changelog

All notable changes to this plugin will be documented here.

## 1.1.0 — staging copy

- Copied from `cursor/plugins` `third_party/finance` at 90b852d (version 1.1.0).
- Renamed to `finance-staging` / "Finance (Staging)" so it can be installed alongside the public `finance` plugin.
- MCP server URL changed to the xAI staging connectors gateway.
- Everything else (client gating, logo, license, server key `finance`) matches upstream.
